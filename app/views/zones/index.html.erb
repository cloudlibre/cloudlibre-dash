<!-- <p id="notice"><%= notice %></p>

<h1>Zones</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @zones.each do |zone| %>
      <tr>
        <td><%= zone.name %></td>
        <td><%= link_to 'Show', zone %></td>
        <td><%= link_to 'Edit', edit_zone_path(zone) %></td>
        <td><%= link_to 'Destroy', zone, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Zone', new_zone_path %>
 -->
<div class="row">
  <div class="col-md-8">
    <form class="form-inline block">
      <div class="input-group mb-2 mr-sm-2" style="width:100%">

        <label class="sr-only" for="search">Search domain names</label>
        <input class="form-control my-1 mr-sm-2" type="search" placeholder="Search websites in my account" aria-label="Search" name="search">
        <button class="btn btn-outline-success my-1 mr-sm-2" type="submit">Search</button>
      </div>
    </form>
  </div>
  <div class="col-md-2">
    <%= link_to "+ Add Site", new_zone_path, class: "btn btn-primary" %>
  </div>
</div>

All zones:

<%- @zones.in_groups_of(2).each do |zones| %>
  <div class="row">
    <%- zones.each do |zone| %>
    <%- if zone %>
      <div class="col">
        <%= link_to zone.name, zone.name %>
      </div>
      <%- end %>
    <%- end %>
  </div>
<%- end %>


<%= @zones.in_groups_of(2) %>
